function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 

	var random;

	random = Math.floor(Math.random() * 11);

	if (random!==0) {
		alert(random);

	}
	
	/*
	var random;
	random = parseInt(Math.random() * 11);
	if (random!=0){

		alert(random);
	}
	*/

}//FIN DE LA FUNCIÓN